/**
 * 应用级稳定配置。
 *
 * 仅存放不随运行时变化的静态常量（应用名、窗口尺寸等）。
 * 引擎相关配置放在 engine/engineConfig.ts，路径推导放在 paths/paths.ts。
 */

/** 应用名称，用于窗口标题等。 */
export const APP_NAME = "相机边框工具"

/** 主窗口默认宽度。 */
export const WINDOW_DEFAULT_WIDTH = 1400

/** 主窗口默认高度。 */
export const WINDOW_DEFAULT_HEIGHT = 920

/** 主窗口最小宽度。 */
export const WINDOW_MIN_WIDTH = 1200

/** 主窗口最小高度。 */
export const WINDOW_MIN_HEIGHT = 760

/** 主窗口背景色。 */
export const WINDOW_BACKGROUND_COLOR = "#f6f1e8"
